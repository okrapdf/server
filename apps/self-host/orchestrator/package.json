{
  "name": "okra-self-host-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "okra self-host orchestrator (workerd + Agents): durable DocumentAgent that drives parser containers.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --ip 0.0.0.0 --port 8787 --persist-to /data"
  },
  "dependencies": {
    "agents": "^0.12.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
