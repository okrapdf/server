{
  "id": "review.redactions",
  "name": "Redaction Review UI",
  "version": "0.1.0",
  "description": "Opens a review gate for redaction proposals before burn-in or export.",
  "kind": "review_ui",
  "runtime": "http_base_url",
  "runtime_config": {
    "endpoint_path": "/ui/reviews/redactions/:documentId"
  },
  "input_schema": {
    "type": "object",
    "required": ["document_id", "redactions"],
    "properties": {
      "document_id": {
        "type": "string"
      },
      "redactions": {
        "$ref": "okra-document-graph/v1#/redactions"
      }
    }
  },
  "output_schema": {
    "$ref": "okra-document-graph/v1#/reviews"
  },
  "reads": ["document", "pages", "redactions", "artifacts"],
  "writes": ["reviews", "lineage"],
  "license": "MIT",
  "commercial_ok": true,
  "network_policy": {
    "mode": "restricted"
  },
  "isolation": {
    "namespace": "okra.capabilities.review.redactions",
    "mode": "worker",
    "network_namespace": "none",
    "filesystem": {
      "read_only_root": true,
      "writable_paths": [],
      "volume_mounts": []
    },
    "secrets": {
      "scope": "none"
    },
    "notes": "Human review route over local graph state; no outbound network."
  },
  "offline_ok": true,
  "metadata": {
    "surface": "capability",
    "recipe_role": "human_redaction_review"
  }
}
